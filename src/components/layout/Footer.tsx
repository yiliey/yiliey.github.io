'use client';

/**
 * Intentionally renders nothing — the site ends on its own content rather than
 * a footer. Kept as a component so bringing one back is a one-file change.
 */
export default function Footer() {
  return null;
}
